interface:
  display_name: "Valueages"
  short_description: "Repo-specific patterns and workflows for valueages"
  default_prompt: "Use the valueages repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true